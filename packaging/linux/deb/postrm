#!/bin/bash
# Post-removal script for simple-rsyncd

set -e

PROJECT_NAME="simple-rsyncd"
CONFIG_DIR="/etc/$simple-rsyncd"
LOG_DIR="/var/log/$simple-rsyncd"

if [ "$1" = "purge" ]; then
    # Remove configuration files (optional - user may want to keep them)
    # Uncomment to remove config on purge:
    # rm -rf "$CONFIG_DIR"
    
    # Remove log files
    rm -rf "$LOG_DIR"
    
    # Remove service user (optional)
    # userdel rsyncddev 2>/dev/null || true
fi

exit 0

